# The four double-click launchers must stay CRLF: cmd.exe misparses some constructs
# (multi-line blocks, labels) under bare LF, and the whole point of these files is a
# Windows user who never opens a terminal. Everything else follows git's default text
# handling.
*.cmd text eol=crlf
